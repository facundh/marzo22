import React from 'react'
import { Rider } from '../../constantes'

const Modal = (data) => {
  return (
    <>
        <h1>Reportes `${data.rider}`</h1>
        <p> data rider </p>
        <button>Confirmar</button>
       
    </>
  )
}

export default Modal